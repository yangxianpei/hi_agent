# Backend (FastAPI)

## 运行方式

1. 安装依赖（示例）：
   - `pip install -e .`
2. 启动服务：
   - `uvicorn main:app --reload`
3. 打开文档：
   - Swagger UI: `http://127.0.0.1:8000/docs`
   - ReDoc: `http://127.0.0.1:8000/redoc`

我来为您查询从杭州东站到西湖景区的驾车和地铁两种出行方案
sudo docker run -it -p 3000:3000 --entrypoint /bin/bash a8f5319dc765