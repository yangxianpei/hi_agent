services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hi-agent-backend
    ports:
      - "3000:3000"
    volumes:
      - /home/ubuntu/hi_agent/backend:/app
    env_file:
      - .env
    restart: unless-stopped
